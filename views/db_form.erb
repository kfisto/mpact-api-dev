<!DOCTYPE html>
<html>
<head>

<script src="http://code.jquery.com/jquery-2.1.1.min.js" type="text/javascript"></script>

<script>
var entries = [];
<% guide_entries.each do |entry| %>
	entries.push(<%= entry.to_json %>);
<% end %>

$(document).ready(function() {

	$('#entry').change(function () {
		var value = $('select option:selected').val();
		$('#name').val(entries[value-1].name);
		$('#image').val(entries[value-1].image);
		$('#dfcontent').val(entries[value-1].data);
	});

	$('#name').val(entries[0].name);
	$('#image').val(entries[0].image);
	$('#dfcontent').val(entries[0].data);

	// $('#deleteBtn').click(function() {
	// 	if (confirm('Delete ' + $('#name').val() + ' from the database?')) {

	// 		// $('#_method').val('delete');

	// 		var url = '/guide/entry/' + $('#entry').val();

	// 		console.log(url);

	// 		$.ajax({
	// 			type: 'DELETE',
	// 			url: url,
	// 			success: function(result) {
	// 				debugger;
	// 			},
	// 			failure: function(result) {
	// 				debugger;
	// 			}
	// 		});
	// 	} else {
	// 		$('#msgline').html('Delete canceled.');
	// 	}
	// });

});

</script>

<style>
html,body {
	font-family: Helvetica,Arial,sans-serif;
	font-size: .9em;
}
form {
	width: 400px;
}
label {
	display: inline-block;
	width: 100px;
	text-align: right;
}
input[type="submit"],input[type="reset"] {
	float: right;
}
</style>

</head>
<body>
<form action="/post/entry" method="post" enctype="multipart/form-data">
	<label for="entry">Entry</label>
	<select id="entry" name="entry">
		<% guide_entries.each do |entry| %>
		<option value="<%= entry[:id] %>"><%= entry[:name] %></option>
		<% end %>
	</select><br/>

	<label for="name">Name</label>
	<input id="name" name="name"/><br/>

	<label for="image">Photo</label>
	<input id="image" name="image" size="20" /><br/>

	<label for="datafile">Data File</label>
	<input type="file" name="datafile"/>

	<textarea id="dfcontent" rows="6" cols="75"></textarea>

	<br/><br/>
	<input type="reset"/>
	<input type="submit"/>

	<!-- <input type="hidden" id="_method" name="_method" value="put"> -->
	<input type="button" id="deleteBtn" value="Delete"/>
</form>

<div id="msgline"></div>

</body>
</html>



